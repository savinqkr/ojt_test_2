import 'package:flutter/material.dart';

class SearchTask extends StatelessWidget {
  const SearchTask({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blueAccent,
      child: const Text('SearchTask'),
    );
  }
}
