import 'package:flutter/material.dart';

class ErrorConditions extends StatelessWidget {
  const ErrorConditions({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.yellow,
      child: const Text('ErrorConditions'),
    );
  }
}
