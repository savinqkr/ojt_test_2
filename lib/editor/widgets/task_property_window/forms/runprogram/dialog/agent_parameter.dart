import 'package:flutter/material.dart';

class AgentParameter extends StatelessWidget {
  const AgentParameter({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blueAccent,
      child: const Text('AgentParameter'),
    );
  }
}
